// import { useAuthStore, useErrorStore } from '@symfoititis-frontend-monorepo/stores'
// import { useAvailabilityStore } from '../stores/availability'
// import { useBookingStore } from '../stores/bookings'
// import { useChatStore } from '@symfoititis-frontend-monorepo/stores' 

export const useTeacherFetch = () => {
  // const authStore = useAuthStore()
  // const errorStore = useErrorStore()
  // const availabilityStore = useAvailabilityStore()
  // const bookingStore = useBookingStore()
  // const chatStore = useChatStore()

  // const getChatMessages = async (cid: number, participant_id: string) => {
  //   try {
  //     await chatStore.getChatMessages(cid, participant_id)
  //   } catch (err: any) {
  //     errorStore.addError(JSON.parse(err.message))
  //   }
  // }

  // const getAvailabilitySlots = async (cid: number) => {
  //   try {
  //     await availabilityStore.getAvailabilitySlots(cid)
  //   } catch (err: any) {
  //     errorStore.addError(JSON.parse(err.message))
  //   }
  // }

  // const addAvailabilitySlots = async () => {
  //   try {
  //     await availabilityStore.addAvailabilitySlots()
  //   } catch (err: any) {
  //     errorStore.addError(JSON.parse(err.message))
  //   }
  // }

  // const updateAvailabilitySlots = async () => {
  //   try {
  //     await availabilityStore.updateAvailabilitySlots()
  //   } catch (err: any) {
  //     errorStore.addError(JSON.parse(err.message))
  //   }
  // }

  // const cancelAvailabilitySlots = async () => {
  //   try {
  //     await availabilityStore.cancelAvailabilitySlots()
  //   } catch (err: any) {
  //     errorStore.addError(JSON.parse(err.message))
  //   }
  // }



  // const getBookings = async () => {
  //   try {
  //     await bookingStore.getBookings()
  //   } catch (err: any) {
  //     errorStore.addError(JSON.parse(err.message))
  //   }
  // }

  // const cancelBooking = async (b_id: number) => {
  //   try {
  //     await authStore.updateToken(420)
  //     await bookingStore.cancelBooking(b_id)
  //     await getBookings()
  //   } catch (err: any) {
  //     errorStore.addError(JSON.parse(err.message))
  //   }
  // }

  // return { getChatMessages, getAvailabilitySlots, updateAvailabilitySlots, cancelAvailabilitySlots, saveAvailabilityChanges, getBookings, cancelBooking }
}
